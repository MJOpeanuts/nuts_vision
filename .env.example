# Database Configuration
# Copy this file to .env and adjust values as needed

# Database host (use 'localhost' if running locally, or container name if in Docker network)
DB_HOST=localhost

# Database port
DB_PORT=5432

# Database name
DB_NAME=nuts_vision

# Database credentials
DB_USER=nuts_user
DB_PASSWORD=nuts_password
